<%- include(`${partialsPath}/head`, {
	description: 'JW Player web demos',
	title: 'JW Player web demos',
	isIndex: true
}); %>
<div class="ds-page">
	<div class="container">
		<ul>
			<% configs.forEach(c => { %><a href="<%= c.slug %>/"><li><%= c.title %><p><%= c.description %></p></li></a><% }); %>
		</ul>
	</div>
</div>
<%- include(`${partialsPath}/footer`, { isIndex: true }); -%>
